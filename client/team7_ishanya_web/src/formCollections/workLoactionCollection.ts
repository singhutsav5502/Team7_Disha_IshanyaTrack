import { createListCollection } from "@chakra-ui/react";

export const workLocationCollection = createListCollection({
    items: [
      { label: "Foundation", value: "Foundation" },
      { label: "Academy", value: "Academy" }
    ]
  });